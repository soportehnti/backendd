<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ETC LATAM — Ticket {{id}}</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            user-select: none;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 0.9em;
            margin-top: 2rem;
            display: flex;
            justify-content: center;
        }

        .main-container {
            width: 800px;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem;
            height: 100px;
        }

        .emit-style {
            font-size: 1em;
            margin-top: 5rem;
            font-weight: bold;
        }

        .emit-style i {
            font-weight: lighter;
            font-size: 1rem;
        }


        .container-title-emit {
            justify-content: space-between;
        }

        .title {
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }

        .img-logo {
            width: 130px;
            height: auto;
        }

        .container-body {
            padding: 1rem;
            flex: 1;
            height: 500px;
        }

        h2,
        .title-description,
        .title-subject,
        .title-device {
            font-size: 1.2rem;
        }

        .container-descripcion {
            /* background: yellow;   */
            margin-bottom: 2rem;
        }

        .description-paragraph,
        .subject-paragraph,
        .device-number {
            font-size: 1rem;
            margin-top: 0.5rem;
            text-align: justify;
        }

        .container-device {
            /* background: red; */
            display: flex;
            align-items: end;
            justify-content: end;
        }

        .title-device {
            margin-right: 0.5rem;
        }

        .device-number {
            font-size: 1.2rem;
            font-weight: bold;
        }


        /* ######## BODY FOOTER ########*/

        .container-footer {
            /* background: RED; */
            padding: 1rem;
        }

        .container-footer div {
            margin-bottom: 0.5rem;
        }
    </style>
</head>

<body>
    <div class="main-container">
        <header>
            <div class="container-title-emit">
                <h1 class="title">ETC LATAM</h1>
                <span class="emit-style">Emitido: <i>{{created_at}}</i></span>
                <br>
                <span class="emit-style">Id del Ticket: <i>{{id}}</i></span>
            </div>
            <img class="img-logo" src="http://localhost:3000/public/assets/etc-latam-logo.png" alt="etc latam logo">
        </header>

        <section class="container-body">
            <div class="container-descripcion">
                <h2 class="title-description">Asunto</h2>
                <p class="description-paragraph">
                    {{subject}}
                </p>
            </div>

            <div class="container-descripcion">
                <h2 class="title-subject">Descripción</h2>
                <p class="description-paragraph">
                    {{description}}
                </p>
            </div>

            <div class="container-device">
                <h2>Dispositivo del Cliente:</h2>
                <span class="device-number"><i>{{device}}</i></span>
            </div>

        </section>

        <footer class="container-footer">
            <div>
                <span>Solicitado por:</span>
                <span>{{requestor_id}}</span>
            </div>
            <div>
                <span>Generado para:</span>
                <span>{{client_id}}</span>
            </div>
        </footer>
    </div>
</body>

</html>